# Production-readiness checklist

I want Cascade to become a real Kafka replacement, but it isn't there yet. This is the checklist I'm using to keep that goal honest and measurable.

## What is working now

- Kafka TCP framing with a small, explicit API and version matrix.
- Topic creation, metadata, produce, fetch, and offset lookup in single-node and dynamically reconfigurable cluster modes.
- Kafka magic-v2 batches stored without decoding or recompressing them.
- Size-based segments, restart discovery, partial-tail recovery, and synchronous or batched periodic flushing.
- Classic consumer-group join, sync, heartbeat, leave, rebalance, and session expiry.
- Kafka-compatible `ListGroups` v0-v4, `DescribeGroups` v0-v4, and `DeleteGroups` v0-v1 over an immutable acknowledged group view, with state filters, owner routing, per-group ACLs, active-group protection, and atomic offset cleanup.
- A CRC32C-protected committed-offset journal that is forced to disk and recovered after restart.
- Idempotent producer IDs, epoch fencing, bounded duplicate detection, sequence validation and recovery, transactions, timeouts, transactional offsets, and `read_committed` isolation in single-node and clustered modes.
- Metadata images committed by stable or dual joint majorities, durable voter endpoints and directory IDs, controller terms and votes, quorum election, controller leases, broker fencing, round-robin replica assignment, leader epochs, ISR failure detection, and promotion of a surviving replica.
- Parallel synchronous leader-to-follower replication, `min.insync.replicas` checks, committed high watermarks, and leader-only fetch and offset lookup.
- Checksum-protected, double-buffered partition high-watermark checkpoints that recover conservatively after torn or corrupt writes.
- Incremental replica recovery that verifies a chained batch fingerprint, truncates only the divergent suffix, transfers bounded committed chunks while Produce continues through the existing ISR, briefly fences the final delta, and re-admits the replica after a quorum metadata commit.
- Durable online partition reassignment with learners outside the ISR, add-before-remove ordering, atomic target finalization, replacement plans, cancellation, and controller-failover resume.
- Kafka-compatible `AlterPartitionReassignments` v0 and `ListPartitionReassignments` v0 Admin APIs.
- Kafka-compatible `DescribeQuorum` v0-v2, `AddRaftVoter` v0-v1, and `RemoveRaftVoter` v0 Admin APIs.
- Observer synchronization, joint-consensus admission/removal, interrupted-transition resume, partition-drain validation, and active-controller handoff.
- Versioned coordinator state for classic-group membership and assignments, committed offsets, producer fencing, active transaction ranges, outcomes, and transactional offsets, using the metadata path through format 11 and per-shard quorum journals at format 12.
- Stale coordinator-image rejection, rollback on quorum loss, ready-owner group/offset expiry, controller-owned transaction expiry, and atomic transaction/outcome offset checkpoints.
- Deterministic directional peer partitions and protocol-triggered message drops without using timing as the fault trigger.
- Clean/unclean startup markers plus real subprocess force-kill tests that bypass broker shutdown hooks.
- Exact data, transaction, producer-epoch, and committed-offset recovery after force kill, including conservative truncation of torn data, offset, and delivery journal tails.
- Stable-quorum majority service, minority coordinator fencing, durable joint-transition resume, controller loss during joint consensus, and dual-majority write rejection tests.
- Scheduled time/size retention that retires only closed committed segments and preserves log-end offset continuity across broker restart.
- Atomic `.deleted` and `.cleaned` rename protocols with startup completion for interrupted segment deletion, compaction, and coordinator-journal replacement.
- Record-level key compaction for uncompressed and gzip non-transactional batches, with CRC32C-safe batch rewriting, tombstone grace, cleanup bandwidth throttling, and conservative retention of Snappy/LZ4/Zstd, control, and transactional batches.
- Rebuilt batch timestamp and transaction-range indexes, durable offset expiry, bounded offset/delivery/cluster-metadata journals, and quorum-committed per-topic cleanup and retention policy exposed through Kafka Admin.
- Pre-append free-space admission that leaves the log end unchanged and returns Kafka `KAFKA_STORAGE_ERROR` to clients.
- TLS 1.2/1.3 listeners with PKCS12/JKS keys, optional client-certificate verification, and Kafka-compatible authentication using PLAIN, SCRAM-SHA-256/512, or OAUTHBEARER; bearer tokens require `SASL_SSL`.
- Hostname-verified mutual TLS for controller, metadata, replication, and recovery RPCs, with a distinct certificate identity bound to every claimed node ID.
- Atomic, bounded key/trust-store fingerprinting and reload for client/peer TLS, with last-valid context retention, readiness/events/metrics, established-session continuity, and generation-aware peer reconnection.
- Atomic peer-identity policy reload that preserves the last valid snapshot, supports overlapping rotation identities, fails readiness on malformed replacements, and audits every accepted or denied internal request.
- Offline salted SCRAM verifiers, salted PBKDF2-SHA-256 PLAIN credentials, and signed OAuth/OIDC JWTs validated against bounded RSA, EC, or Ed25519 JWKS, exact issuer/audience/time/scope policy, operator-approved claim-to-role mapping, constant-time password verification, bounded exchanges, expiring connection principals, mechanism-aware audit/metrics, and atomic credential/key rotation that preserves the last valid snapshot and gates readiness on malformed replacements.
- Deny-by-default topic, group, transaction, and cluster ACLs with exact/prefix/wildcard matching, explicit-deny precedence, super users, and live atomic policy rotation.
- Forced JSONL authentication/authorization audit events with escaped fields, source address, TLS state, principal, decision, operation, and resource.
- Kafka-compatible ACL describe/create/delete administration with atomic durable policy publication.
- Global and per-IP connection caps, a bounded in-flight request semaphore, immediate overload shedding, and per-principal ingress, egress, Produce, and Fetch token buckets whose configured rate and burst are conservatively divided across the current quorum.
- A separate loopback-default operations listener with bearer authentication for remote binds, liveness/readiness/status JSON, and bounded-label Prometheus 0.0.4 metrics.
- Readiness gates for broker state, fencing, flush backlog, disk reserve, structured-log health, TLS material, peer identity policy, and credential policy.
- Size-rotated JSONL broker events plus deduplicated connection, request, flush-backlog, and disk-capacity alert/resolution events.
- Kafka-compatible `DescribeConfigs` v2 for non-sensitive, read-only broker and effective topic configuration, verified through Kafka Admin 4.3.1.
- Kafka-compatible `IncrementalAlterConfigs` v0 for validated, quorum-committed per-topic cleanup and retention overrides.
- Offline backup creation plus a broker write-barrier online snapshot path, both with an exact SHA-256 manifest, forced copies, and atomic publication; verification and restore reject tampering, extra files, traversal, symlinks, and existing targets.
- Peer capability exchange, a quorum-committed feature-level image, metadata-format negotiation through format 12, and a committed compatibility floor that survives controller loss while prospective voters still require a live compatibility check.
- Feature-gated immutable shard objects, atomic journal references, bounded/checksummed recovery, complete shard-aware backup/restore, conservative post-checkpoint reclamation, and metadata-service fencing after publication failures. Heartbeat reconciliation cannot mistake an in-flight follower-first commit for a stale controller.
- Rendezvous-hashed group and transaction coordinator ownership with quorum-committed broker eligibility, monotonic coordinator-image installation, and quorum-durable state.
- Feature-gated shard deltas with 64 group buckets, 64 transaction buckets, one monotonic producer allocator, independent conflict versions, atomic multi-shard validation, bounded coalescing installation, shard-aware readiness, and bounded-label checkpoint metrics.
- Feature-gated incremental metadata journal records and peer commits, exact-base rejection, snapshot fallback, bounded replay checkpoints, and measured disk/wire byte counters for formats 10–11.
- Bounded controller-local FIFO publication batching that validates shard deltas independently, combines compatible cross-owner work into one quorum proposal, preserves atomic multi-shard changes, bounds retained requests/bytes, drains safely, and exposes node-only metrics for formats 9–11.
- A format-12 coordinator quorum with typed routing, sorted shard locking, bounded admission, majority-forced prepare/vote/certificate/finalize records, cross-node certified recovery, stale-owner voter reconciliation, CRC32C per-shard journals, atomic checkpoint compaction, monotonic image installation, and node-only metrics. Steady-state coordinator writes bypass the metadata log.
- Bounded FIFO offset commit batching with count/estimated-byte admission, queue deadlines, ownership and static-member revalidation, atomic rollback/read isolation, snapshot-aware shutdown draining, node-only metrics, and single-decision OffsetFetch readiness. The format-12 32-worker campaign preserves exact offsets through failover and restart without quorum or batch failures.
- Immutable typed snapshot preparation, fixed-layout content-validated shard encoding caches, image-local decoded views, domain-isolated checkpoint deltas, cached/indexed committed-offset reads, session-preserving group reconciliation, and safe wakeup of blocked sync requests. Group and delivery mutations use separate monitors.
- Immutable acknowledged offset and delivery read views with per-group/per-producer indexes, atomic old-or-new publication, one-view-per-response consistency, blocked-finalization tests, node-only metrics, and exact 1,000-group failover/restart evidence. Read decisions no longer use the mutation monitor.
- Kafka `ConsumerGroupHeartbeat` v0 with server-side range/uniform assignment, member epochs, static membership, expiry, persisted assignments, deterministic topic IDs, and Metadata v9-v12 interoperability with Kafka 4.3.1.
- A configurable multi-tenant soak runner with exact payload/offset validation and JSON evidence, plus a two-phase physical power-loss probe whose acknowledgements are forced to an independent witness device before power is cut externally.
- A Kafka-client disaster-recovery test that produces records, shuts down, backs up, restores into a new data directory, starts in clean-recovery mode, and consumes the exact values.
- A three-broker Kafka-client security test that rotates old CA trust to overlap, replaces every peer leaf, removes the old CA, replicates over each TLS generation, stops the original broker/controller, resumes `acks=all` production on the majority, and consumes the exact committed sequence.
- A minimal Alpine/musl JDK 21 image design using UID/GID 65532, a module-limited runtime, an internal readiness probe, read-only-root operation, and persistent volumes. The current 1.3.1 Linux/amd64 image's qualification and publication status are recorded in the [release notes](releases/1.3.1.md). The earlier 39.2 MB measurement belongs to 1.2.0, not this release. Publication now requires a zero-finding security scan and reuses the tested image archive. ARM64 publication is disabled until its runtime qualification is added.
- A hardened three-broker Kubernetes topology with StatefulSets, disruption protection, anti-affinity, default-deny networking, secret-backed monitoring, ServiceMonitor discovery, Prometheus rules, and a Grafana dashboard.
- Real Kafka 4.3.1 client tests for Admin, ACL administration, per-topic configuration, Producer, explicit Consumer, subscribed consumers, static-member fencing, rebalancing, committed-offset recovery, broker restart, three/four-node replication, partition-leader shutdown, controller/coordinator loss, stale-term rejection, post-election metadata changes, reassignment cancellation, reassignment through controller loss, voter admission, active-controller removal/restart, and an open transaction completed after failover.
- Real Kafka 4.3.1 `SASL_SSL` Admin/Producer/classic-group Consumer tests for SCRAM-SHA-256/512 and OAUTHBEARER that cross authentication, verifier/JWKS rotation, wrong-token denial, ACL, storage, offset commit, TLS trust, metrics, readiness, and audit paths.
- Exact-count load tests at one million and ten million records with latency, CPU, GC, heap, storage, and flush metrics.
- Deterministic malformed-frame fuzzing plus exact external client checks for KafkaJS, confluent-kafka Python, franz-go, and Confluent.Kafka .NET; the 1.0.0 API-range contract cannot narrow silently.

## What I still need before a production release

| Area | What must pass before release | Where Cascade is now |
| --- | --- | --- |
| Availability | At least three brokers, replicated partitions, ISR tracking, leader election, and verified recovery from broker, process, and disk loss | Graceful and forced-kill recovery, persisted high-watermarks, replica re-admission, stable/joint partition tests, and online reassignment pass; physical power and device-loss qualification remains |
| Metadata | Durable quorum controller, fencing, leader epochs, reassignment, and cluster membership changes | Durable election, leases, fencing, metadata images, leader epochs, resumable reassignment, joint consensus, deterministic partition recovery, dual-majority safety, peer capability exchange, quorum-committed feature levels, and the pinned 1.0.0-to-1.1.0 rolling/rollback campaign pass; exhaustive external failure qualification remains |
| Delivery semantics | Idempotent producers, sequence validation, transactions, producer-state recovery, and `read_committed` isolation | Kafka-client failover, forced-kill/torn-tail recovery, atomic transaction/offset shard conflicts, certified cross-node decision recovery, compacted shard-journal recovery, separate group/delivery mutation, and format-12 quorum/restart campaigns pass; high-cardinality transaction/membership churn on dedicated hardware remains |
| Storage lifecycle | Time and size retention, log and offset compaction, timestamp and transaction indexes, disk-pressure handling, and safe deletion | Quorum-committed retention/cleanup, record-level uncompressed/gzip compaction, tombstone grace, cleanup throttling, rebuilt indexes, bounded journals, atomic replacement recovery, and low-disk rejection pass; Snappy/LZ4/Zstd rewriting, replicated cleanup coordination, and external disk-failure qualification remain |
| Security | TLS, SASL mechanisms, authorization and ACLs, audit events, and secret rotation | Client TLS, hostname-verified peer mTLS, certificate-bound nodes, Kafka PLAIN/SCRAM/OAUTHBEARER, RSA/EC/Ed25519 JWKS, approved role mapping, Kafka ACL Admin APIs, forced JSONL audit, and live secret/policy rotation pass; opaque-token introspection, automatic discovery/revocation, and multi-tenant qualification remain |
| Resource isolation | Client and user quotas, request and connection limits, bounded queues, overload shedding, and multi-tenant tests | Connection caps, bounded requests, request-frame limits, Kafka throttle fields, immediate shedding, and conservative quorum-split principal quotas pass; safe unused-share reclamation and an archived multi-day authenticated soak remain |
| Operations | Metrics, health and readiness endpoints, structured logs, admin API coverage, backup and restore, container/orchestrator deployment, and capacity alerts | Metrics/health/status, rotating events, capacity signals, topic configuration, offline backup and local online write-barrier snapshots, distroless/Compose/Kubernetes artifacts, monitoring assets, and exact recovery tests pass; one coordinated artifact per replica host, external TLS/mTLS, scheduled retention, and recurring restore drills remain |
| Compatibility | Supported-version contract, malformed-frame and fuzz tests, multiple client languages, and a rolling upgrade and downgrade matrix | Exact API ranges, Metadata v4-v12 schema decoding, fuzzing, five client languages, and the pinned format-11-to-format-12 source campaign pass; the published image pair, cross-version idempotent retries, broader client versions, and future release pairs remain |
| Consumer groups | New consumer protocol, static-member fencing, administrative group APIs, offset retention and compaction, and coordinator failover | Classic groups plus `ConsumerGroupHeartbeat` v0, server-side assignment, member epochs, static fencing, expiry/compaction, rendezvous ownership, nonblocking acknowledged offset/group reads, and list/describe/delete Admin APIs pass restart, ACL, and coordinator-failover tests; later heartbeat versions, `ConsumerGroupDescribe`, offset administration, membership churn at scale, and dedicated-host capacity evidence remain |
| Qualification | Multi-day soak tests, crash, kill, and power-failure simulation, network partitions, disk-full and corruption tests, and repeatable dedicated-host benchmarks | Deterministic partitions/drops, force kills, torn-tail recovery, disk-full admission, a configurable soak runner, and an independent-witness physical probe exist; the smoke run passes, but the 72-hour run, real power/device cuts, arbitrary packet impairment, and dedicated-host RF=3 campaign have not been executed |

## When I will call it production ready

I keep the latest coordinator evidence in the [2026-09-12 coordinator completion qualification](performance/2026-09-12-coordinator-completion.md).

I won't describe a release as a Kafka replacement until every blocking row above has an automated acceptance test and passes on the deployment topology I document.

When I publish a performance result, I'll include the hardware, durability policy, workload, client configuration, and exact delivery count. When I claim compatibility, I'll list the API keys and versions instead of just saying "Kafka compatible."

The most recent complete Scala/Kafka-Java run passed **539/539** tests with no failures or skips in **153 seconds**. It includes the combined transaction-plus-group-offset failover regression, certified recovery, bounded journal checkpoints, stale-owner reconciliation, and independent service mutation tests. The focused independent-coordinator matrix previously passed **81/81**, and the legacy format-11 publication tests passed **8/8** after their fixture explicitly pinned the path they qualify. The Linux storage/coordinator campaign previously passed **79/79**, including actual directory forcing and reclamation; Windows still conservatively retains object history when forcing is unavailable.

The controller publication campaign commits 3,000 writes across 1,000 groups and recovers 1,000/1,000 exact offsets after controller failover and full restart. It combines 1,675 requests into 1,156 batches with zero queue rejection. The 57.934 writes/s and 4,714.624 ms p99 are not production-capacity evidence or a matched comparison. The [publication report](performance/2026-09-05-coordinator-publication.md) records the host, retries, metrics, limitations, and full regression results.

The acknowledged-read campaign commits 3,000 writes and records 3,000 acknowledged offset snapshots while preserving 1,000/1,000 final offsets through controller failover and full restart. It has zero connection, offset-batch, or publication admission rejections. Its 104.495 writes/s and 3,122.982 ms p99 use different publication settings from earlier runs, so I do not claim a speedup. The [read-isolation report](performance/2026-09-07-coordinator-read-isolation.md) records the exact result and remaining boundary.

The latest format-12 32-worker campaign commits **3,000 writes across 1,000 resident groups** and preserves every final offset through controller failover and full restart. It commits **668/668** shard-quorum transactions with zero quorum failure, admission rejection, batch failure, checkpoint failure, or torn-tail truncation. It measures **283.180 writes/s** with **283.373 ms p99**. The source rolling campaign from pinned format 11 to format 12 passes all ten phases and exact 40/40 recovery. These are not controlled production capacity measurements. The [dated report](performance/2026-09-12-coordinator-completion.md) contains the workload and boundaries.

The latest default-path regression consumed exactly **1,000,000 / 1,000,000** records at **468,358 produced records/s** and **463,448 consumed records/s**. This is single-node plaintext with periodic flushing, not RF=3 synchronous durability or TLS capacity. The earlier ten-million result remains historical. All five external languages passed exact 25-record checks against the earlier published image. Release 1.4.0 still needs exact-image runtime/client/cluster qualification and a fresh zero-finding scan before publication. I will not call Cascade production ready until the multi-day and physical-hardware campaigns, arbitrary-network impairment, restore drills, and dedicated-host RF=3 capacity have archived evidence.
